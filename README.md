# sticky_bnns

Installation 
```aiignore
# Clone repo
git clone https://github.com/ltronneb/sticky_bnns.git
# Install requirements
pip install -r requirements.txt
# Install the package
pip install -e .
```